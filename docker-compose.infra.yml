# Infrastructure-only compose file for local development.
# Run: docker compose -f docker-compose.infra.yml up -d
# Then start the app via the "LocalInfra" launch profile in IDE or dotnet run.

services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: coworking_rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmqdata:/var/lib/rabbitmq
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  db:
    image: postgres:16-alpine
    container_name: coworking_db
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

#  redis:
#    image: redis:7-alpine
#    container_name: coworking_redis
#    command: redis-server --requirepass ${REDIS_PASSWORD}
#    ports:
#      - "6379:6379"
#    volumes:
#      - redisdata:/data
#    restart: unless-stopped

volumes:
  pgdata:
  rabbitmqdata:
#  redisdata:
